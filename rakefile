task :default => [:book]

desc "build a book"
task :book => :build do
  sh "xsltproc -o book.fo book.xsl _site/book.html"
  sh "fop -fo book.fo -pdf book.pdf"
  sh "evince book.pdf &"
end

desc "clean and build site using Jekyll"
task :build do
  sh "rm -rf _site/"
  sh "jekyll"
end

